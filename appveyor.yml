version: '{branch}-{build}'

init:
  # Gather some basic build environment details.
  #- rem set
  #- dir "%ProgramFiles(x86)%\Microsoft Visual Studio *" /b /o
  #- dir "%ProgramFiles%\Microsoft SDKs\Windows" /b /o
  #- dir "%SystemDrive%\Qt\5.*" /b /o

environment:
  VSVER: 14.0 # Visual Studio 2015
  matrix:
    #- QTDIR: C:\Qt\5.3\mingw482_32
    #- QTDIR: C:\Qt\5.4\mingw491_32
    #- QTDIR: C:\Qt\5.6\mingw49_32
    #- QTDIR: C:\Qt\5.6\msvc2015
    - QTDIR: C:\Qt\5.6\msvc2015_64

configuration:
  #- debug
  - release

install:
  - '%QTDIR%\bin\qtenv2.bat'
  - cmd: choco install opencv
  #- cmd: choco install cmake
  - set QTDIR
  #- qmake -v
  - if %QTDIR:_64=%==%QTDIR% ( set ARCH=x86 ) else set ARCH=x64
  - if %QTDIR:msvc=%==%QTDIR% g++ --version
  - if %QTDIR:msvc=%==%QTDIR% set make=mingw32-make.exe
  - if %QTDIR:msvc=%==%QTDIR% %make% --version
  - if not %QTDIR:msvc=%==%QTDIR% call "%ProgramFiles(x86)%\Microsoft Visual Studio %VSVER%\VC\vcvarsall.bat" %ARCH%
  - if not %QTDIR:msvc=%==%QTDIR% set make=nmake.exe
  - if not %QTDIR:msvc=%==%QTDIR% %make% /? > nul

  #- set QTDIR
before_build:
  #- mkdir %APPVEYOR_BUILD_FOLDER%-build
  #- cd %APPVEYOR_BUILD_FOLDER%-build
  - cd %APPVEYOR_BUILD_FOLDER% 
  #- qmake -Wall -Wlogic -Wparser CONFIG+=%CONFIGURATION% %APPVEYOR_BUILD_FOLDER%
   
build_script:
  - cmd: set OpenCV_DIR=C:\tools\opencv
  - cmd: set PATH=C:\tools\opencv\build\bin:%PATH%
  - cmd: cmake %APPVEYOR_BUILD_FOLDER% C:\projects\hello-qt

  #- dir c:\tools\ /s /b
  #- '%make%'
  
  #- dir %APPVEYOR_BUILD_FOLDER%\out
artifacts:
#- path: ..\hello-qt-build\$(configuration)\Giteye.exe
  - path: $(configuration)\Giteye.exe
    name: Giteye
