os: unstable
install:
  - cmd: mkdir opencvbuild
  - cmd: cd opencvbuild
  - cmd: mkdir build
  - cmd: mkdir source
  - cmd: choco install opencv
  - cmd: cd ..
  - set QTDIR=C:\Qt\5.6\mingw49_32
  - set PATH=%PATH%;%QTDIR%\bin;C:\MinGW\bin
build_script:
  - qmake Giteye.pro
  - mingw32-make